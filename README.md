# demo-rest-api

Demo Spring Boot REST API (Java 17, Spring Boot 3.3.3, Maven)

## Run

Import into Eclipse as Existing Maven Project or run:

```
./mvnw spring-boot:run
```

Endpoint: `GET /api/hello` -> returns `Hello, World!`
